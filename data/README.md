# Data Directory

Raw data is excluded from Git (see .gitignore).

## Datasets used
Download with:
    kaggle datasets download -d aadarshmalviya/my-defect-collection-folders -p data/raw/defect-collection --unzip
    kaggle datasets download -d wednesday233/corrosion-detect-dataset -p data/raw/corrosion-detect --unzip
    kaggle datasets download -d ayanzadeh93/color-classification -p data/raw/color-classification --unzip
